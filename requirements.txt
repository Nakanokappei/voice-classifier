# Core
openai>=1.30.0
python-dotenv>=1.0.0

# Data
pandas>=2.1.0
numpy>=1.26.0

# Clustering / metrics
scikit-learn>=1.4.0
hdbscan>=0.8.33

# Graph-based clustering (HNSW k-NN graph + Leiden community detection).
# All three are optional: the tuner falls back gracefully when any are missing.
hnswlib>=0.8.0
python-igraph>=0.11.0
leidenalg>=0.10.0

# UX
tqdm>=4.66.0

# Markdown → HTML 変換（--format html / both で使用）
markdown>=3.5

# Testing (dev)
pytest>=8.0.0
