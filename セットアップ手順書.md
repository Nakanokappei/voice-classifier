# voice-classifier セットアップ＆実行手順書（Windows用 / ユーザー向け）

このプログラム **voice-classifier** は、顧客からの問い合わせCSVを読み込み、
内容が似たものを自動でグループ分け（クラスタリング）して、
チャットボット用のカテゴリ案とレポートを作るツールです。

このドキュメントの通りに**上から順にコピー＆ペースト**すれば動きます。
専門知識は不要です。考えて選ぶ箇所はありません（Azureの接続情報を入れる1か所を除く）。

> **このドキュメントはWindowsのユーザー名が `yamada` の例です。**
> もしあなたのPCのユーザー名が違う場合は、以下に出てくる
> `C:\Users\yamada` の **`yamada` の部分を、あなたのユーザー名に置き換えて**ください。
> （ユーザー名がわからないときは、PowerShellで `echo $env:USERNAME` と打つと表示されます）

---

## 0. 完成形（どこに何ができるか）

最終的に、あなたのPCの中はこうなります（すべて自動で作られます）：

```
C:\Users\yamada\
├── download.py                                  ← 手順2で作成（プログラム取得用の小さな道具）
└── voice-classifier\                            ← 本体（自動で作られる）
    ├── src\pipeline.py                          ← 実際に動くプログラム本体
    ├── .env                                     ← 手順4で作成（Azure接続情報を書く）
    ├── requirements.txt                         ← 必要な部品リスト（最初から入っている）
    └── data\
        ├── input\サンプルデータ.csv             ← 手順5で置く「分析したいCSV」
        └── output\                              ← 手順6で結果が自動生成される
```

> このドキュメントは、CSVに **「問い合わせ内容」という列がある** データ用です。
> （列名が違う場合だけ、最後の「困ったとき」を見てください）

---

## 1. Python（パイソン）を入れる

このプログラムを動かすための土台ソフトです。一度入れれば次回から不要です。

1. ブラウザで次のページを開く → **https://www.python.org/downloads/windows/**
2. 一番上の **「Download Python 3.x.x」** ボタンを押してインストーラを保存
3. ダウンロードした `python-3.x.x-amd64.exe` をダブルクリック
4. **【最重要】** 最初の画面の下にある
   **「Add python.exe to PATH」** のチェックボックスに**必ずチェックを入れる**
5. **「Install Now」** を押す → 完了したら閉じる

### 確認（入ったかどうか）

スタートメニューで「**PowerShell**」と入力し、**「Windows PowerShell」** を開きます。
青い画面が出たら、次の1行を貼り付けて Enter：

```powershell
python --version
```

`Python 3.x.x` のように表示されればOKです。
（`'python' は…認識されていません` と出たら、手順1-4のチェックを忘れています。Pythonを入れ直してください）

> **以降の手順は、この「Windows PowerShell」の青い画面に貼り付けていきます。**
> 貼り付けは画面内で**右クリック**、実行は **Enter** です。

---

## 2. プログラム本体をダウンロードする

下のブロックを**まるごとコピーして**PowerShellに貼り付け、Enter。
（インターネット経由でプログラム一式を取得し、`voice-classifier` フォルダを作ります）

```powershell
cd C:\Users\yamada
@'
import urllib.request, zipfile, io, os
URL = "https://codeload.github.com/Nakanokappei/voice-classifier/zip/refs/heads/main"
print("Downloading:", URL)
req = urllib.request.Request(URL, headers={"User-Agent": "python-urllib"})
with urllib.request.urlopen(req, timeout=60) as resp:
    data = resp.read()
print(f"Downloaded: {len(data):,} bytes")
with zipfile.ZipFile(io.BytesIO(data)) as zf:
    zf.extractall(".")
if os.path.exists("voice-classifier-main") and not os.path.exists("voice-classifier"):
    os.rename("voice-classifier-main", "voice-classifier")
print("Done -> extracted to ./voice-classifier")
'@ | Set-Content -Path "C:\Users\yamada\download.py" -Encoding ascii
python "C:\Users\yamada\download.py"
```

`Done -> extracted to ./voice-classifier` と表示されれば成功です。

---

## 3. 必要な部品を組み込む（インストール）

下のブロックをまるごと貼り付けてEnter。**数分かかります**（部品を自動で集めています）。
途中で大量の文字が流れますが、最後に `Successfully installed ...` と出れば成功です。

```powershell
cd C:\Users\yamada\voice-classifier
python -m venv .venv
.\.venv\Scripts\python.exe -m pip install --upgrade pip
.\.venv\Scripts\python.exe -m pip install -r requirements.txt
```

---

## 4. Azureの接続情報を設定する（ここだけ入力が必要）

下のブロックを貼り付ける**前に**、最初の2行の `★` の部分を
**あなたのAzureの値に書き換えて**ください。

- `$apiKey` … Azureの **APIキー**
- `$endpoint` … `https://（あなたのリソース名）.openai.azure.com` の形

> 下の3つ（embed / namer / advisor）は、Azure側のデプロイ名が
> `text-embedding-3-small` と `gpt-4o` の場合はそのままでOKです。
> 違う名前で作っている場合、例えばnamerに”gpt-4o-mini”を使っているときは、その名前に書き換えてください。

書き換えたら、ブロックをまるごと貼り付けてEnter：

```powershell
$apiKey   = "★ここにAzureのAPIキーを貼り付け★"
$endpoint = "★https://あなたのリソース名.openai.azure.com★"
$embed    = "text-embedding-3-small"
$namer    = "gpt-4o"
$advisor  = "gpt-4o"

Set-Content -Path "C:\Users\yamada\voice-classifier\.env" -Encoding ascii -Value @(
  "AZURE_OPENAI_API_KEY=$apiKey",
  "AZURE_OPENAI_ENDPOINT=$endpoint",
  "AZURE_OPENAI_EMBEDDING_DEPLOYMENT=$embed",
  "AZURE_OPENAI_NAMER_DEPLOYMENT=$namer",
  "AZURE_OPENAI_ADVISOR_DEPLOYMENT=$advisor"
)
Write-Output ".env を作成しました"
```

`.env を作成しました` と出ればOKです。

---

## 5. 分析したいCSVを置く

下のブロックの1行目 `★` を、**分析したいCSVファイルのフルパス**に書き換えてください。

> フルパスの簡単な調べ方：エクスプローラーでCSVを **Shift＋右クリック →
> 「パスのコピー」**。それを `★` の位置に貼り付けます（前後の `"` はそのまま）。

書き換えたら、ブロックをまるごと貼り付けてEnter。
（CSVを所定の場所に `サンプルデータ.csv` という名前でコピーします）

```powershell
$source = "★ここに元のCSVのフルパスを貼り付け★"
New-Item -ItemType Directory -Force -Path "C:\Users\yamada\voice-classifier\data\input" | Out-Null
Copy-Item $source "C:\Users\yamada\voice-classifier\data\input\サンプルデータ.csv" -Force
Write-Output "CSVを配置しました"
```

---

## 6. 実行する

下のブロックをまるごと貼り付けてEnter。これが本番です。
（埋め込み→自動グループ分け→ラベル付け→レポート作成、まで一気に行います）

```powershell
cd C:\Users\yamada\voice-classifier
$env:PYTHONUTF8 = "1"
.\.venv\Scripts\python.exe src\pipeline.py --input "data\input\サンプルデータ.csv" --text-col "問い合わせ内容" --target chatbot --format both
```

`All steps complete: ...` と表示されれば成功です。

### このコマンドの意味（オプションの解説）

コマンドの後半にある `--◯◯` の部分は「設定」です。今回はこの4つを指定しています。
**通常はこのままでOK**です（変える必要があるとすれば「分析する列」と「用途」だけ）。

| 指定している設定 | 意味 | 変えるとしたら |
|---|---|---|
| `--input "data\input\サンプルデータ.csv"` | **分析するCSVファイル**の場所。手順5で置いたファイルを指しています。 | 別のCSVを分析するとき。ただし手順5でファイルを差し替える運用なら、ここは変えなくてOK |
| `--text-col "問い合わせ内容"` | CSVの**どの列の文章をAIに読ませて分類するか**。ここでは顧客の問い合わせ本文の列を指定しています。 | CSVの列名が「問い合わせ内容」でない場合だけ、実際の列名に変更 |
| `--target chatbot` | **何のために分類するか（最適化の方針）**。`chatbot` はチャットボット向けに、意図（intent）を細かく分けようとします。 | 用途を変えるとき。`faq`（FAQページ向け・やや大きめの分類）/ `insight`（傾向分析向け）も選べます |
| `--format both` | **レポートの出力形式**。`both` で見やすいHTMLと、テキストのMarkdownの**両方**を出します。 | HTMLだけでよければ `html`、Markdownだけなら `md` |

> 補足：上記以外にも細かい設定（分類数の上限など）がありますが、指定しなければ
> 自動で最適な値が選ばれます。最初は触らなくて問題ありません。

---

## 7. 結果を見る

下のブロックを貼り付けてEnter。**最新の結果レポートが自動でブラウザで開きます**。

```powershell
$latest = Get-ChildItem "C:\Users\yamada\voice-classifier\data\output" -Directory | Sort-Object LastWriteTime | Select-Object -Last 1
Start-Process (Join-Path $latest.FullName "report.html")
```

結果フォルダには次のファイルが入っています：

| ファイル | 内容 |
|---|---|
| `report.html` | グループ分け結果（カテゴリ名・代表的な問い合わせ）★まずこれを見る |
| `parameter_search.html` | 分析の詳しい根拠＋AIによる解説コメント |
| `clusters.csv` | カテゴリ一覧（Excelで開ける） |
| `サンプルデータ_classified.csv` | 元データに「どのカテゴリか」を付け足したもの |

---

## 次回以降（2回目から）

手順1〜5は不要です。PowerShellを開いて、**手順6と7だけ**を実行すれば動きます。
別のCSVを分析したいときは、手順5でCSVを差し替えてから手順6・7を実行してください。

---

## 困ったとき（エラーが出た場合）

| 症状 | 対処 |
|---|---|
| `'python' は…認識されていません` | 手順1の「Add python.exe to PATH」チェック漏れ。Pythonを入れ直す |
| 手順3で `hdbscan` のエラーで止まる | 次を貼り付けて実行 → `cd C:\Users\yamada\voice-classifier; .\.venv\Scripts\python.exe -m pip install hdbscan --only-binary=:all:` その後もう一度手順3 |
| 実行時に「認証エラー」「401」「Connection」等 | 手順4の `.env` の値（APIキー・エンドポイント・デプロイ名）を見直して手順4をやり直す |
| 「No text column candidates」やコラム名のエラー | CSVに「問い合わせ内容」という列が無い。手順6のコマンドの `--text-col "問い合わせ内容"` を、実際の列名に書き換える |
| ダウンロード（手順2）やインストール（手順3）が通信エラーで止まる | 社内ネットワークが GitHub / PyPI への通信を制限している可能性。情報システム部門に確認 |

それでも解決しない場合は、PowerShellに出ている**赤い文字のエラー全文**をコピーして共有してください。
